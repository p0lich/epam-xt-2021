SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE [dbo].[User_GetById]
	@id int
AS
BEGIN
	SET NOCOUNT ON;

	SELECT TOP 1
		Id, Name, DateOfBirth, Password
		FROM Users
		WHERE Id = @id
END
GO
